server:
  port: 8080
springdoc:
  api-docs:
    path: "/api/v1/api-docs"
  swagger-ui:
    path: "/api/v1/docs"
    operationsSorter: "alpha"
    tagsSorter: "alpha"
routes:
  paths:
    user: "/api/v1/usuarios"
    userId: "/api/v1/usuarios"
external:
  service:
    authentication: "http://localhost:8080/api/v1"
adapters:
  r2dbc:
    host: "localhost"
    port: 5434
    database: "crediya_autenticacion_db"
    schema: "public"
    username: "crediya_user"
    password: "C0ntr4s3n4C0mp13j4"
spring:
  application:
    name: "authentication"
  devtools:
    add-properties: false
  h2:
    console:
      enabled: true
      path: "/h2"
  profiles:
    include: null
management:
  endpoints:
    web:
      exposure:
        include: "health,prometheus"
  endpoint:
    health:
      probes:
        enabled: true
cors:
  allowed-origins: "http://localhost:4200,http://localhost:8080"
